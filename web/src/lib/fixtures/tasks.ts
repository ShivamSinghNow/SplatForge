import type { TaskOption } from '../api/types';

export const DEMO_TASKS: TaskOption[] = [
  {
    id: 'pick_mug',
    name: 'Pick Mug',
    description: 'Pick up the mug from the table and place it in the goal zone.',
  },
];
